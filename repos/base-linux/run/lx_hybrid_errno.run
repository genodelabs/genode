build { core init lib/ld test/lx_hybrid_errno }

create_boot_directory

install_config {
config
+ parent-provides
  + service ROM
  + service LOG
  + service PD
  + service CPU

+ default-route | + any-service | + parent

+ start test-lx_hybrid_errno | ld: no | caps: 100 | ram: 1M
-
}

build_boot_image [build_artifacts]

run_genode_until "--- finished thread-local errno test ---.*\n" 10

