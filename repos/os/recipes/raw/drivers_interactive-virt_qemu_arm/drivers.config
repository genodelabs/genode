config
+ parent-provides
  + service IRQ
  + service IO_MEM
  + service ROM
  + service PD
  + service CPU
  + service LOG
  + service Timer
  + service Capture
  + service Event

+ default | caps: 100

+ start virtdev_rom | ram: 640K
  + provides | + service ROM
  + route | + any-service | + parent

+ start platform | managing_system: yes | ram: 2M
  + provides | + service Platform
  + route
    + service ROM | label: devices | + child virtdev_rom
    + any-service                  | + parent
  + config
    + policy | label_prefix: virtio_fb       | info: yes | + device gpu0
    + policy | label_prefix: virtio_keyboard | info: yes | + device input0
    + policy | label_prefix: virtio_mouse    | info: yes | + device input1

+ start event_filter | caps: 90 | ram: 1280K
  + provides | + service Event
  + route
    + service ROM | label: config | + parent | label: event_filter.config
    + service ROM                 | + parent
    + service PD                  | + parent
    + service CPU                 | + parent
    + service LOG                 | + parent
    + service Timer               | + parent
    + service Event               | + parent

+ start virtio_keyboard | caps: 70 | ram: 1M
  + binary virtio_mmio_input
  + config | match_product: keyboard
  + route
    + service Platform | + child platform
    + service Event    | + child event_filter | label: keyboard
    + service CPU      | + parent
    + service PD       | + parent
    + service LOG      | + parent
    + service ROM      | + parent

+ start virtio_mouse | caps: 80 | ram: 1M
  + binary virtio_mmio_input
  + config | match_product: mouse
  + route
    + service Platform | + child platform
    + service Event    | + child event_filter | label: mouse
    + service CPU      | + parent
    + service PD       | + parent
    + service LOG      | + parent
    + service ROM      | + parent

+ start virtio_fb | caps: 120 | ram: 50M
  + binary virtio_mmio_fb
  + route
    + service Platform | + child platform
    + any-service      | + parent
-
