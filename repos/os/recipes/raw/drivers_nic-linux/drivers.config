config
+ parent-provides
  + service IRQ
  + service IO_MEM
  + service ROM
  + service PD
  + service RM
  + service CPU
  + service LOG
  + service Timer
  + service Uplink

+ default | caps: 100

+ start nic | ld: no | ram: 4M
  + binary linux_nic
  + route | + any-service | + parent
-
