config
+ parent-provides
  + service IRQ
  + service IO_MEM
  + service ROM
  + service PD
  + service RM
  + service CPU
  + service LOG
  + service Timer
  + service Uplink

+ default | caps: 100

+ start platform | managing_system: yes | ram: 2M
  + provides | + service Platform
  + route | + any-service | + parent
  + config
    + policy | label: nic -> | + device ethernet

+ start nic | ram: 4M
  + binary lan9118_nic
  + route
    + service Platform | + child platform
    + service ROM      | + parent
    + service PD       | + parent
    + service CPU      | + parent
    + service LOG      | + parent
    + service Timer    | + parent
    + service Uplink   | + parent
-
