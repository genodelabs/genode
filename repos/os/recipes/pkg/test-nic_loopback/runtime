runtime | ram: 32M | caps: 1000 | binary: init

+ fail    | after_seconds: 20
+ succeed | : child "test-nic_loopback" exited with exit value 0
+ fail    | : *Error:

+ content
  + rom | label: ld.lib.so
  + rom | label: nic_loopback
  + rom | label: test-nic_loopback

+ config
  + parent-provides
    + service ROM
    + service IRQ
    + service IO_MEM
    + service IO_PORT
    + service PD
    + service RM
    + service CPU
    + service LOG

  + default-route
    + any-service
      + parent
      + any-child

  + default | caps: 100

  + start nic_loopback | ram: 2M
    + provides | + service Nic

  + start test-nic_loopback | ram: 2M
-
