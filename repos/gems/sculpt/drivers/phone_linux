config
+ parent-provides
  + service ROM
  + service PD
  + service CPU
  + service LOG
  + service Timer
  + service Report
  + service Capture
  + service Event

+ start fb_sdl | caps: 100 | ram: 10M | ld: no
  + route
    + service Event | + parent | label: sdl
    + any-service   | + parent

+ start modem | caps: 120 | ram: 1M
  + binary dummy_modem
  + route
    + service ROM | label: config | + parent | label: modem.config
    + service ROM                 | + parent
    + service PD                  | + parent
    + service CPU                 | + parent
    + service LOG                 | + parent
    + service Timer               | + parent
    + service Report              | + parent
-
