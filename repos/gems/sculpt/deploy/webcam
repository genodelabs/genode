config | arch: | info: webcam example

+ common_routes
  + service ROM | label_last: ld.lib.so | + parent
  + service ROM | label_last: init      | + parent
  + service CPU                         | + parent
  + service PD                          | + parent
  + service LOG                         | + parent
  + service Timer                       | + parent
  + service Capture                     | + parent
  + service Gui                         | + parent

+ start internal | ram: 64M | pkg: usb_webcam
  + route
    + service Usb | + child usb
    + any-service | + parent
  + provides
    + service Capture
    + service Report

+ start external | ram: 64M | pkg: usb_webcam
  + route
    + service Usb | + child usb
    + any-service | + parent
  + provides
    + service Capture
    + service Report
-
