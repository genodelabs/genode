runtime | ram: 12M | caps: 300 | binary: init

+ provides | + rom

+ requires | + rm

+ content
  + rom | label: mesa_gpu-etnaviv.lib.so
  + rom | label: cached_fs_rom
  + rom | label: init
  + rom | label: vfs
  + rom | label: vfs.lib.so

+ config | verbose: yes
  + service ROM | + default-policy | + child cached_fs_rom | label: mesa_gpu-etnaviv.lib.so

  + parent-provides
    + service ROM
    + service PD
    + service RM
    + service CPU
    + service LOG

  + default-route
    + any-service
      + parent
      + any-child

  + start vfs | caps: 100 | ram: 1M
    + provides | + service File_system
    + config | verbose: yes
      + vfs | + rom mesa_gpu-etnaviv.lib.so
      + default-policy | root: / | writeable: no

  + start cached_fs_rom | caps: 100 | ram: 9M
    + provides | + service ROM
    + config
-
