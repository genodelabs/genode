runtime | ram: 20M | caps: 300 | binary: init

+ provides | + rom

+ requires | + rm

+ content
  + rom | label: mesa_gpu-softpipe.lib.so
  + rom | label: cached_fs_rom
  + rom | label: init
  + rom | label: vfs
  + rom | label: vfs.lib.so

+ config
  + service ROM | + default-policy | + child cached_fs_rom | label: mesa_gpu-softpipe.lib.so

  + parent-provides
    + service ROM
    + service PD
    + service RM
    + service CPU
    + service LOG

  + default-route
    + any-service
      + parent
      + any-child

  + start vfs | caps: 100 | ram: 5M
    + provides | + service File_system
    + config
      + vfs | + rom mesa_gpu-softpipe.lib.so
      + default-policy | root: / | writeable: no

  + start cached_fs_rom | caps: 100 | ram: 8M
    + provides | + service ROM
    + config
-
