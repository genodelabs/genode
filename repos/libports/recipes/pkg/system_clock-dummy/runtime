runtime | ram: 4M | caps: 300 | binary: init

+ provides | + rtc

+ requires | + timer

+ content
  + rom | label: ld.lib.so
  + rom | label: init
  + rom | label: dummy_rtc
  + rom | label: system_rtc

+ config
  + parent-provides
    + service ROM
    + service PD
    + service RM
    + service CPU
    + service LOG
    + service Timer

  + default | caps: 100 | ram: 1M

  + service Rtc | + default-policy | + child system_rtc

  + start dummy_rtc
    + provides | + service Rtc
    + route | + any-service | + parent
    + config | year:   2021
             | month:  10
             | day:    14
             | hour:   10
             | minute: 0
             | second: 0

  + start system_rtc
    + provides | + service Rtc
    + config
    + route
      + service Rtc | + child dummy_rtc
      + any-service | + parent
-
